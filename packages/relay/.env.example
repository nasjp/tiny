# cp .env.example .env and fill in (do not commit .env)
# Pins the deploy target when wrangler is logged into multiple accounts. Not needed with a single account.
# The value is the Account ID column of `pnpm exec wrangler whoami`.
CLOUDFLARE_ACCOUNT_ID=<your account id>
